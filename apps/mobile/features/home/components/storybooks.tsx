import { View } from "react-native";
import React from "react";
import { SectionHeader } from "./section-header";

const Storybooks = () => {
  return (
    <View>
      <SectionHeader title="New Storybooks" href="/(tabs)/storybooks" />
    </View>
  );
};

export default Storybooks;
