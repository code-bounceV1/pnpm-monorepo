import { View } from "react-native";
import React from "react";
import { SectionHeader } from "./section-header";

const Units = () => {
  return (
    <View>
      <SectionHeader title="Continue Learning" href="/(tabs)/subjects" />
    </View>
  );
};

export default Units;
